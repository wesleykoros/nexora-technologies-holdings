"use client"; import {useState} from "react";
export function ContactForm(){const [state,setState]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState('Sending…');const form=new FormData(e.currentTarget);const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});setState(r.ok?'Thank you. Your enquiry has been received.':'We could not send your message. Please email info@nexoratechholdings.com.');if(r.ok)e.currentTarget.reset()} return <form onSubmit={submit} className="glass rounded-3xl p-7"><div className="grid gap-5 sm:grid-cols-2"><Field name="name" label="Name"/><Field name="email" label="Email" type="email"/><Field name="company" label="Company"/><Field name="subject" label="Enquiry type"/></div><label className="mt-5 block text-sm font-bold">Message<textarea name="message" required rows={7} className="mt-2 w-full rounded-2xl border border-[color:var(--border)] bg-transparent p-4 outline-none focus:border-indigo-500"/></label><button className="mt-5 rounded-full bg-[color:var(--foreground)] px-6 py-3 font-bold text-[color:var(--background)]">Send enquiry</button>{state&&<p className="mt-4 text-sm text-[color:var(--muted)]">{state}</p>}</form>}
function Field({name,label,type='text'}:{name:string,label:string,type?:string}){return <label className="text-sm font-bold">{label}<input required={name==='name'||name==='email'} name={name} type={type} className="mt-2 w-full rounded-2xl border border-[color:var(--border)] bg-transparent px-4 py-3 outline-none focus:border-indigo-500"/></label>}
